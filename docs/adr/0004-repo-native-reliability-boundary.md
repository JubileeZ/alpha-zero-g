# Repo-Native Reliability Boundary

Cross-device reliability must not depend on incompatible Cursor or Antigravity lifecycle hooks. Repo-native validation, continuity commands, git gates, and CI own shared guarantees; IDE hooks remain thin adapters invoking same commands. This avoids duplicated policy semantics and preserves guarantees when users switch device or IDE.

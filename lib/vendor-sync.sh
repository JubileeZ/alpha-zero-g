#!/usr/bin/env bash
# lib/vendor-sync.sh — re-vendor mattpocock/skills
# Shallow-clones upstream and replaces vendor/ content.
# Called by: azg update --vendor
# Sourced by update.sh; do NOT run directly.
# Implemented in Phase 2.

vendor_sync() {
  die "vendor-sync is not yet implemented (Phase 2)."
}

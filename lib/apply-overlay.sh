#!/usr/bin/env bash
# lib/apply-overlay.sh — tool-map remap + ANTIGRAVITY-NOTE injection
# Applied at copy-time for each vendored skill.
# Called by: azg setup
# Sourced by setup.sh; do NOT run directly.
# Implemented in Phase 3.

apply_overlay() {
  die "apply-overlay is not yet implemented (Phase 3)."
}

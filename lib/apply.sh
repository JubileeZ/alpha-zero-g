#!/usr/bin/env bash
# lib/apply.sh — azg apply
# Retrofits existing projects with the azg harness.
# Sourced by the azg dispatcher; do NOT run directly.
# Implemented in Phase 7.

cmd_apply() {
  die "azg apply is not yet implemented (Phase 7)."
}

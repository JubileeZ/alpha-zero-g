---
name: fable-loop
description: Optional Fable Loop stub — invoke only when evaluating core+Fable treatment
---

# Fable Loop (stub)

This is a **placeholder** skill so `azg fable sync` can install an opt-in project skill tree before a real upstream is wired.

## When to use

- Only on projects that opted in with `azg fable sync`
- Only as the **core+Fable** arm of Evaluation Suite comparisons (ADR 0005)

## When not to use

- Do not treat this as proven Reliable Delivery
- Do not enable in global `azg setup` core profile
